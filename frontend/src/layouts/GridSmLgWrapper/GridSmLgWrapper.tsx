import React from 'react';

export function GridSmLgWrapper({ children }: { children: React.ReactNode }) {
    return (
        <div className={'grid-sm-lg-wrapper'}>
            {children}
        </div>
    )
}
